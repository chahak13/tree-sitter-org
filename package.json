{
  "dependencies": {
    "nan": "^2.15.0"
  },
  "name": "tree-sitter-org",
  "version": "1.0.0",
  "description": "Tree-sitter for org-mode",
  "main": "bindings/node",
  "scripts": {
    "build": "tree-sitter generate && node-gyp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tree-sitter-cli": "^0.20.1"
  }
}
